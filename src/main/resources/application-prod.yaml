spring:
  datasource:
    url: jdbc:mysql://${SQL_HOST}:${SQL_PORT}/${SQL_DATABASE}?sslMode=REQUIRED
    username: ${SQL_USER}
    password: ${SQL_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
